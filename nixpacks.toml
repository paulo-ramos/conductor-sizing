[phases.setup]
nixPkgs = ["...", "fontconfig", "freetype", "harfbuzz"]

[phases.build]
cmds = ["dotnet restore", "dotnet publish src/ConductorSizing.Web/ConductorSizing.Web.csproj -c Release -o out"]

[start]
cmd = "dotnet out/ConductorSizing.Web.dll"
